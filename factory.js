const isValidDate = (date) => true;
const dateDiff = (startDate , endDate) => true;
const getHash = (password) => "some hash password"+password;
const canCarBeBooked = (bookingDates , issueDate,  retuenDate) => true;

module.exports = {
    isValidDate,
    dateDiff,
    getHash,
    canCarBeBooked
}